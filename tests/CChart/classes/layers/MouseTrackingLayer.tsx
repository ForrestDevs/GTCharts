
// import GTChart from './../../GTChart';

// class MouseTrackingLayer {
//     constructor(private chart: GTChart) {
//         super();
//         // Assuming the chart element or its container can be accessed here
//         // Add event listeners to track mouse movement
//         document.addEventListener('mousemove', this.handleMouseMove);
//     }

//     handleMouseMove = (event: MouseEvent) => {
//         const rect = (event.target as HTMLCanvasElement).getBoundingClientRect();
//         const x = event.clientX - rect.left;
//         const y = event.clientY - rect.top;
//         this.chart.updateMousePosition(x, y);
//     }
// }

// export default MouseTrackingLayer;